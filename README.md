# Modelling steady state behaviour of a PN Junction
All the simulation parameters are initialized in `inc/init.h`

## Build and run
Run the executable:
```bash
./run
```

## Visualize the exported data
Run:
```bash
./visualize potential
./visualize concentration
```
